import { server } from '../serverInstance'

export async function _createExercice(exercice) {
  return exercice
}

export async function _updateExercice(exerciceId) {
  return exerciceId
}

export async function _removeExercice(exerciceId) {
  return exerciceId
}

export async function getExercicesBy(exercice) {
  return exercice
}
